// Test projected linesearch on a simple quadratic with a linear constraint

import optimize3

fn func(x, y, z) {
    return (x-0.5)^2 + 0.5*(y-1)^2 + 2*(z-2)^2
}

fn g(x, y, z) {
    return x + y - 1
}

var start = Matrix([0.5,0.5,0])

var adapt = FunctionAdapter(func, start=start, constraints = [g])

var control = ProjectedLineSearchController(adapt)

control.optimize(100)

print "Solution:"
print adapt.get()